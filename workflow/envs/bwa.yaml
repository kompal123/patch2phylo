# workflow/envs/bwa.yaml
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - bwa=0.7.19
  - samtools=1.20
  - htslib=1.20
  - libdeflate
  - zlib      # let solver pick 1.3.x
  - xz
  - pigz

