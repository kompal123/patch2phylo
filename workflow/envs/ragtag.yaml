# workflow/envs/ragtag.yaml
name: ragtag
channels: [bioconda, conda-forge]
dependencies:
  - ragtag
  - minimap2
  - seqkit
  - blast            # optional but nice to have
  - python           # inherited

